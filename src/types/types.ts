export interface WeatherData_Props {
  city: string;
  current_temp: number;
  current_weather_icon: string;
  current_weather: string;
  current_time: string;
  temp_max: number;
  temp_min: number;
  humadity: number;
  cloudy: number;
  wind_speed: number;
}